import { SearchPostPipe } from './search-post.pipe';

describe('SearchPostPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPostPipe();
    expect(pipe).toBeTruthy();
  });
});
